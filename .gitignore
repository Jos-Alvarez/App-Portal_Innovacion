# Dependencias
node_modules/
.pnp
.pnp.js
.yarn/install-state.gz

# Build de Next.js y salidas de compilacion
.next/
out/
build/
dist/
*.tsbuildinfo
next-env.d.ts

# Tests y cobertura
coverage/
*.lcov
.nyc_output/

# Variables de entorno: se ignora todo menos la plantilla versionada
.env
.env.*
!.env.example

# Logs de gestores de paquetes
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
.pnpm-debug.log*

# Salida del servidor corriendo. La escribe el servicio que arranca
# `start_app.bat` en el servidor, y describe una ejecucion concreta en una
# maquina concreta: versionarla mezclaria el registro de un entorno con el
# codigo de todos.
logs/

# Despliegue
.vercel

# Ruido del sistema operativo
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# Editores
.idea/
*.swp

# Cache de herramientas locales. Se regenera sola y describe la maquina de
# quien la genero, no el proyecto: versionarla solo produce conflictos que no
# significan nada.
.atl/

# Nota: `prisma/migrations/` NO se ignora — el historial de migraciones se
# versiona.

# Sentinela del harness de verificacion documental (efimero, no se versiona)
.claude/.verificacion-documental.lock
